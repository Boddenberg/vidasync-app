/**
 * Configurações globais do app
 *
 * URLs, timeouts, versão... tudo centralizado.
 * Nunca espalhe URLs ou valores "mágicos" por vários arquivos.
 */

/** URL base do BFF (backend) */
export const API_BASE_URL = 'https://vidasync-bff-production.up.railway.app';

/** Nome do app */
export const APP_NAME = 'VidaSync';

/** Ano atual (para footer, etc.) */
export const APP_YEAR = 2026;
